% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_matrix.R
\name{get_item_matrix}
\alias{get_item_matrix}
\title{Convert Student Responses to Item Score Matrix}
\usage{
get_item_matrix(students_responses, responses = FALSE)
}
\arguments{
\item{students_responses}{A data frame containing student responses with at least
the following columns:
\itemize{
\item \code{student_id}: Character, unique identifier for each student.
\item \code{item_id}: Numeric, unique identifier for each test item.
\item \code{score}: Numeric, score obtained by the student on the item.
}}

\item{responses}{Logical. If \code{FALSE}, the matrix contains scored items (numeric values).
If \code{TRUE}, it contains raw responses (character values).}
}
\value{
A data frame (wide format) where:
\itemize{
\item Rows represent unique students.
\item Columns represent test items.
\item Values represent the students' scores on each item.
}
}
\description{
This function transforms the \code{students_responses} data frame into a rectangular format
where each row represents a student and each column represents an item, with values
corresponding to the students' scores.
}
\details{
\itemize{
\item Removes rows where \code{score} is missing (\code{NA}).
\item Renames \code{item_id} using the helper function \code{name_item()}, which should return
a standardized item name (e.g., "Item_1" instead of numeric IDs).
\item Uses \code{pivot_wider()} to transform the data into a matrix-like format.
}
}
